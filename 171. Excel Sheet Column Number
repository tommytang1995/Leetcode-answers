public class Solution {
    public int titleToNumber(String s) {
        int res = 0;
        for(int i = 0; i < s.length; i++){
            res = res * 26 + (s.charAt(i) - 'a' + 1);
        }
        return res;
    }
}

It's just like 100 * (1 - 0) + 10 * (2 - 0) + 0 * (1 - 0) = 121
